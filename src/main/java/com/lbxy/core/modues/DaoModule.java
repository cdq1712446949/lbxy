package com.lbxy.core.modues;

import com.google.inject.AbstractModule;
import com.google.inject.Singleton;
import com.lbxy.dao.*;

/**
 * @author lmy
 * @description DaoModule
 * @date 2018/9/19
 */
public class DaoModule extends AbstractModule {
    @Override
    protected void configure() {
//        bind(BillDao.class).to(BillDao.class).in(Singleton.class);
//        bind(FleaDao.class).to(FleaDao.class).in(Singleton.class);
//        bind(ImageDao.class).to(ImageDao.class).in(Singleton.class);
//        bind(LostFoundDao.class).to(LostFoundDao.class).in(Singleton.class);
//        bind(ManagerDao.class).to(ManagerDao.class).in(Singleton.class);
//        bind(NotificationDao.class).to(NotificationDao.class).in(Singleton.class);
//        bind(OrderDao.class).to(OrderDao.class).in(Singleton.class);
//        bind(TreeHoleDao.class).to(TreeHoleDao.class).in(Singleton.class);
//        bind(UserDao.class).to(UserDao.class).in(Singleton.class);
    }
}
